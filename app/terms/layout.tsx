import { publicMetadata } from "@/lib/seo";
export const metadata = publicMetadata("Terms of Service | sitescope.fyi", "Terms for using sitescope.fyi website audits and services.", "/terms");
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
