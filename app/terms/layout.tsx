import { publicMetadata } from "@/lib/seo";
export const metadata = publicMetadata("Terms of Service | SiteScope", "Terms for using SiteScope website audits and services.", "/terms");
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
