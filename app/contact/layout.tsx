import { publicMetadata } from "@/lib/seo";
export const metadata = publicMetadata("Contact sitescope.fyi | sitescope.fyi", "Discuss website optimization, redesign or a custom website project.", "/contact");
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
