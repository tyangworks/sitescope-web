import { publicMetadata } from "@/lib/seo";
export const metadata = publicMetadata("Website Optimization & Build Services | SiteScope", "Get implementation help for website optimization, redesign and custom growth analysis.", "/services");
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
