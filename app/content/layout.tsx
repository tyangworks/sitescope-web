import { publicMetadata } from "@/lib/seo";
export const metadata = publicMetadata("Website Growth, SEO & GEO Guides | sitescope.fyi", "Practical guides to search visibility, AI-search readiness and website conversion.", "/content");
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
