import { publicMetadata } from "@/lib/seo";
export const metadata = publicMetadata("Privacy Policy | SiteScope", "How SiteScope handles website audit and account information.", "/privacy");
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
