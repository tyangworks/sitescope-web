import { publicMetadata } from "@/lib/seo";
export const metadata = publicMetadata("Privacy Policy | sitescope.fyi", "How sitescope.fyi handles website audit and account information.", "/privacy");
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
