import { publicMetadata } from "@/lib/seo";
export const metadata = publicMetadata("Pro Website Audit | SiteScope", "Get a one-time Pro audit with detailed SEO and GEO findings and an implementation plan.", "/pro-audit");
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
