# Scoring / Public Reports / Contact Rollout

Operator explicitly approved both migrations and validated production deployment
on 2026-09-06. No live payment, credential rotation or production report deletion
is authorized by this record.

## Baseline and Recoverability

- Web rollback: deployment 7DVLaH62ErScFaVCJNtoSpMNjQk4, commit
  3809bc82e7f2f8ec78da2d5b64e0ca47d6e27549.
- Crawler rollback: /home/admin/releases/sitescope-crawler/c7fabaa297cf2d6cc578e99bb8418a462c8c20fd.
- Database dashboard: Free plan, no platform backups. Prior operator acceptance
  of proceeding without backups remains applicable. Additive columns are retained
  on rollback; no data is removed. Permissions require reviewed forward repair,
  not reopening raw data access. Application rollback must remain BFF-compatible.
- Preflight reports: 43 rows; UUID id, nullable UUID user_id, nullable text
  anonymous_token_hash, nullable JSONB search_visibility. No publication/source
  columns before migration.
- RLS enabled. Policies: Allow public access (ALL true/true), Enable insert for
  everyone (INSERT true), Enable read for everyone (SELECT true), Public can read
  reports (SELECT true), all applying to public. postgres, anon, authenticated and
  service_role held all seven table privileges. No customer report body exported.

## Database Progress

Applied 202609060001_public_report_previews.sql in a transaction; 43 rows after,
zero explicitly public. Source SHA256:
E49F303C6C854C6302A86B2B9F4F7A9759588262FA2A240D02A4436B87FE66C5.

Transactional synthetic fixture tests passed: owned report redemption, persisted
paid state, repeat-call idempotency, second-report credit reuse denial, wrong-owner
rejection. All fixture writes rolled back; 43 real rows unchanged. RPC execute
privileges: anon=false, authenticated=false, service_role=true. This is not a
multi-connection contention test or end-to-end Stripe webhook test.

Migration #2 was amended locally to remove all four observed policies and revoke
PUBLIC table privileges as well as anon/authenticated. It remains pending until
new Web production BFF verification.

## Crawler Progress

Active commit: 10a730b32df9c98c1620480ece88b7fab5bbb795.
Immutable release: /home/admin/releases/sitescope-crawler/10a730b32df9c98c1620480ece88b7fab5bbb795.
Archived exact Git commit, npm ci (zero audit findings), 13 tests and syntax checks
passed on ECS. Temporary port 4001 passed before activation; candidate process was
removed. Production port 4000 and public version returned the correct commit.
Both apex and www CORS origins passed. PM2 process list saved; shared environment
and Cloudflare routing unchanged. Previous release retained.

## Web Progress

Target: 33fb8128a0584f9c68f5e8fae93ccbce7290b6c6, stabilization branch only.
Preview: 9oEfc8GbY2EH9QzsVS884HTdTZ8T, build Ready.
Production deployment: 5KLgmPk812Ktk9SFBsCMtX95rrD5, build Ready, serving
`https://www.sitescope.fyi`.

Production acceptance passed after promotion:

- Anonymous audit created a disposable test report, set an HttpOnly/Secure/Lax
  ownership cookie, returned three Free issues, and a stranger request returned
  404. No Pro fields were returned.
- Public gallery returned three reports with `nextPage=1`; production report
  BFF returned anonymous access and no `fix_plans`, code snippets or implementation
  steps, even with spoofed `isPaid`/`access` query parameters.
- Anonymous `/api/reports` returned 401. Direct Supabase anon REST access to
  `reports` returned permission denied (`42501`).
- Public route smoke tests passed at 390, 768 and 1440px: 10 routes per width,
  no horizontal overflow, zero console errors, private routes noindex.
- The production contact form saved an explicitly labelled internal rollout test
  as receipt 13 and displayed success without a follow-up page error. This test
  was not a customer inquiry.

RLS lockdown migration #2 is now applied. Postflight: 44 report rows (the extra
row is the disposable audit acceptance report), zero report policies, anon and
authenticated SELECT privileges false, service_role SELECT true, RLS enabled and
forced. Migration #2 source SHA256 after removing all four observed legacy
policies: 4F5374C8C5AE1C77DBB64499DECE532D4F790E14C17C6947CB1D0B12E6C7A594.

Production rollback targets remain the previous Vercel deployment
7DVLaH62ErScFaVCJNtoSpMNjQk4 and crawler release
c7fabaa297cf2d6cc578e99bb8418a462c8c20fd. Do not roll back to a release that
expects raw browser report reads.
